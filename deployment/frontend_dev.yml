apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-react-dev
  labels:
    app: frontend-react-dev
    env: development
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend-react-dev
  template:
    metadata:
      labels:
        app: frontend-react-dev
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - frontend-react-dev
            topologyKey: "kubernetes.io/hostname"
      containers:
      - name: frontend-react-dev
        image: yangju0411/frontend:test
        ports:
          - containerPort: 8080

